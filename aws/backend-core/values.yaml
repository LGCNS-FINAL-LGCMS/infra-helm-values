name: backend-core
namespace: backend
replicas: 1
springContainer:
  image: ghcr.io/lgcns-final-lgcms/core
  version: 0.2
  profile: dev
  imagePullPolicy: Always
  resources:
    requests:
      cpu: "250m"
      memory: "512Mi"
    limits:
      cpu: "500m"
      memory: "1Gi"
service:
  type: NodePort
  port: 8080
hpa:
  enabled: true