apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: lgcms-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 8080
        name: http
        protocol: HTTP
      hosts:
        - "api.lgcms.click"
